/* #2 Tipe Dan Variabel
 deklarasi variabel */
var myNumber = 3;                   // variabel dengan tipe angka (angka)
var myString = "Hello, World!"      // variabel dengan tipe huruf (string)
var myBoolean = true;               // variabel dengan tipe boolean
var newVariable;

// eksekusi variabel
console.log(newVariable);           // outputnya undefined kalo gak diisi
console.log(myNumber+" - "+myString+" - "+myBoolean); //output sesuai deklarasi diatas
